{
  "name": "paradoxes",
  "version": "1.0.0",
  "description": "We've chosen 5 famous mathematical paradoxes to present in a single-page application. The selected paradoxes are:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-install": "cd client && npm install && cd ../server && npm install",
    "heroku-postbuild": "npm run heroku-install && cd client && npm run build && cd ../server && npm start"
  },
  "author": "",
  "license": "ISC"
}
